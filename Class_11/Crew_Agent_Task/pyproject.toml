[project]
name = "crew-agent-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hafiz Rizwan", email = "hafizmr7788@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "crewai[google-genai]",
    "litellm",
    "python-dotenv",
]

[project.scripts]
crew-agent-task = "crew_agent_task:main"
run_crew="crew_agent_task.main:kickoff"

[build-system]
requires = ["uv_build>=0.11.25,<0.12.0"]
build-backend = "uv_build"
